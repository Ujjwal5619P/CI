# name: Context

# on:
#     workflow_dispatch:

# env:
#     MY_ENV_VAR: 'Workflow'
#     MY_OVERWRITTEN_VAR: 'Workflow'

# jobs:
#     echo-data:
#         runs-on: ubuntu-latest
#         env:
#             MY_JOB_VAR: 'job'
#             MY_OVERWRITTEN_VAR: 'job'
#         steps:
#             - name: Display information
#               run: |
#                echo "Event name: ${{ github.event_name }}"
#                echo "Ref: ${{ github.ref_name }}"
#                echo "SHA: ${{ github.sha }}"
#                echo "Actor: ${{ github.actor }}"
#                echo "Workflow: ${{ github.workflow }}"
#                echo "Repository: ${{ github.repository }}"
#                echo "Run ID: ${{ github.run_id }}"
#                echo "Run number: ${{ github.run_number }}"
#                echo "Job name: ${{ github.job }}"

#             - name: Retrieve variable values
#               run: |
#                 echo "Variable value: ${{ vars.MY_VAR}}"

#                         - name: Display environment variables
#                           env:
#                             MY_OVERWRITTEN_VAR: 'step'
#                           run: |
#                             echo "MY_WORKFLOW_VAR: ${{ env.MY_ENV_VAR }}"
#                             echo "MY_JOB_VAR: ${{ env.MY_JOB_VAR }}"
#                             echo "MY_OVERWRITTEN_VAR: ${{ env.MY_OVERWRITTEN_VAR }}"
#             - name: Display environment variables
#               run: |
#                 echo "MY_WORKFLOW_VAR: ${{ MY_ENV_VAR }}"
#                 echo "MY_JOB_VAR: ${{ MY_JOB_VAR }}"
#                 echo "MY_OVERWRITTEN_VAR: ${{ MY_OVERWRITTEN_VAR }}"

#     echo-data-2:
#         runs-on: ubuntu-latest
#         steps:
#             - name: Display environment variables
        



name: Contexts

on:
    push:
        branches:
            - main

env:
    MY_WORKFLOW_VAR: 'workflow'
    MY_OVERWRITTEN_VAR: 'workflow'

jobs:
    echo-data:
        runs-on: ubuntu-latest
        env:
            MY_JOB_VAR: 'job'
            MY_OVERWRITTEN_VAR: 'job'
        steps:
            - name: Display information
              run: |
                echo "Event Name: ${{ github.event_name }}"
                echo "Ref: ${{ github.ref_name }}"
                echo "SHA: ${{ github.sha }}"
                echo "Actor: ${{ github.actor }}"
                echo "Workflow: ${{ github.workflow }}"
                echo "Repository: ${{ github.repository }}"
                echo "Run ID: ${{ github.run_id }}"
                echo "Run Number: ${{ github.run_number }}"
                echo "Job Name: ${{ github.job }}"

            - name: Retreive variable value
              run: |
                echo "Variable value: ${{ vars.MY_VAR }}"

                        - name: Display environment variables
                            env:
                                MY_OVERWRITTEN_VAR: 'step'
                            run: |
                                echo "MY_WORKFLOW_VAR: $MY_WORKFLOW_VAR"
                                echo "MY_JOB_VAR: $MY_JOB_VAR"
                                echo "MY_OVERWRITTEN_VAR: $MY_OVERWRITTEN_VAR"
            - name: Display environment variables
              run: |
                    echo "MY_WORKFLOW_VAR: $MY_WORKFLOW_VAR"
                    echo "MY_JOB_VAR: $MY_JOB_VAR"
                    echo "MY_OVERWRITTEN_VAR: $MY_OVERWRITTEN_VAR"  
    echo-data-2:
        runs-on: ubuntu-latest
        steps:
            - name: Display environment variables
              run: |
                    echo "MY_WORKFLOW_VAR: $MY_WORKFLOW_VAR"
                    echo "MY_JOB_VAR: $MY_JOB_VAR"
                    echo "MY_OVERWRITTEN_VAR: $MY_OVERWRITTEN_VAR"